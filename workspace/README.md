# `workspace/` — snapshots of Ollie's workspace doctrine files

Versioned copies of files that live in `/home/openclaw/.openclaw/workspace/`
on the box (WSL `OpenClawGateway` on `<TAILSCALE_IP>`). `AGENTS.md` is Ollie's
operating manual, read by the OpenClaw agent each session; it defines the
background-jobs doctrine (`job-submit.sh`, see `../ollie-jobs/`). Edited live
on the box; this is a snapshot, fetched 2026-06-09 (stable across two reads).
